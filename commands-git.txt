Pour cloner un repository:
Dans le terminal : git clone copie GitHub/url copiée

Procédure de push:
git add . (ajoute tous les fichiers du repertoire courant)
git commit -m 'texte' 
git push 

commande pour pull:
git fetch all
